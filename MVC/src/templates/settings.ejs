<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Settings</title>
	<link rel="stylesheet" href="/styles/dist/output.css">
	<link rel="shortcut icon" href="/favicon/favicon.png" type="image/x-icon">
	<script src="/scripts/dist/onlineSocket.js" defer></script>
</head>

<body class="bg-gray-900 text-white min-h-screen flex flex-col">
	<%- include('components/navbar.ejs') %>
	<%- include('components/errorPopup.ejs') %>
	<%- include('components/successPopup.ejs') %>

	<main class="grow flex flex-col justify-center relative py-6 sm:py-10">
		<div class="container mx-auto px-4 sm:px-6 py-4 sm:py-8 max-w-4xl">
			<h1 class="text-3xl sm:text-4xl font-bold text-center mb-6 sm:mb-8">Settings</h1>

			<!-- Security Settings -->
			<section class="mb-8">
				<h2 class="text-xl sm:text-2xl font-bold mb-4 sm:mb-6 border-b border-gray-600 pb-2">Security Settings</h2>
				<div class="space-y-4">
					<div class="bg-gray-800 p-4 sm:p-6 rounded-lg shadow-md">
						<div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-4">
							<div>
								<h3 class="text-lg sm:text-xl font-semibold mb-2">Two-Factor Authentication</h3>
								<p class="text-gray-300 text-sm sm:text-base">Add an extra layer of security to your account with 2FA</p>
							</div>
							<a href="/2FA" 
								class="bg-blue-600 hover:bg-blue-700 active:bg-blue-900 px-5 sm:px-6 py-2.5 sm:py-3 rounded-lg transition font-semibold text-sm sm:text-base w-full sm:w-auto text-center">
								Configure 2FA
							</a>
						</div>
					</div>
				</div>
			</section>
		</div>
	</main>

	<%- include('components/footer.ejs') %>
</body>
</html>